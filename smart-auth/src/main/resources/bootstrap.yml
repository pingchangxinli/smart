spring:
  cloud:
    config:
      uri: http://localhost:8200
      profile: ${SMART_CONFIG_PROFILE:dev}
      label: master
  application:
    name: smart-auth
server:
  port: 8400
eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:8761/eureka/
