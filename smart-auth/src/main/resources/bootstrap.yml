spring:
  cloud:
    config:
      uri: http://localhost:8200
      profile: dev
      label: master
  application:
    name: smart-auth
server:
  port: 8400
eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:8761/eureka/
