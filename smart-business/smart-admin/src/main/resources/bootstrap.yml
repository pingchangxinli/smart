server:
  port: 8600

spring:
  cloud:
    config:
      uri: http://localhost:8200
      profile: dev
      label: master
  application:
    name: smart-admin
  datasource:
    jdbc-url: jdbc:mysql://localhost:3306/smart_auth?autoReconnect=true&useUnicode=true&characterEncoding=utf8&serverTimezone=UTC
    username: root
    password: root
mybatis-plus:
  type-enums-package: com.lee
