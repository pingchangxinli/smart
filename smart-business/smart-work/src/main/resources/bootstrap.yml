spring:
  cloud:
    config:
      uri: http://smart-config:2080
      profile: ${SMART_CONFIG_PROFILE:dev}
      label: master
  application:
    name: smart-work
server:
  port: 6080
mybatis-plus:
  type-enums-package: com.lee.enums

