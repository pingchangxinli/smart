spring:
  cloud:
    ##配置文件服务
    config:
      uri: http://smart-config:8200
      profile: ${SMART_CONFIG_PROFILE:dev}
      label: master
  application:
    name: smart-gateway
server:
  port: 8300

