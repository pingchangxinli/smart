server:
  port: 50001
spring:
  application:
    name: smart-uid-server
  datasource:
    url: jdbc:mysql://localhost:3306/generate_id?serverTimezone=UTC
    username: root
    password: root
  main:
    allow-bean-definition-overriding: true
eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:8761/eureka/